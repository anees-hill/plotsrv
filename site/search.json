{"config":{"separator":"[\\s\\-_,:!=\\[\\]()\\\\\"`/]+|\\.(?!\\d)"},"items":[{"location":"","level":1,"title":"plotsrv","text":"<p>plotsrv is a lightweight browser UI for observing what your Python processes are producing.</p> <p>It lets you publish plots, tables, logs, JSON, markdown, HTML, images, tracebacks, and files to a local or server-side web UI with very little setup.</p> <p>It is especially useful when you have Python scripts, ETL jobs, experiments, inference processes, or headless server workflows where you want to see what is happening without building a dashboard application.</p> <p>Note</p> <p>plotsrv is not a dashboard framework. It is a low-friction way to observe what Python processes are producing.</p>","path":["plotsrv"],"tags":[]},{"location":"#install","level":2,"title":"Install","text":"pipuv <pre><code>pip install plotsrv\n</code></pre> <pre><code>uv add plotsrv\n</code></pre>","path":["plotsrv"],"tags":[]},{"location":"#quick-start","level":2,"title":"Quick start","text":"quickstart.py<pre><code>import polars as pl\nimport plotsrv as ps\n\ndf = pl.DataFrame({\n    \"name\": [\"alpha\", \"beta\", \"gamma\"],\n    \"value\": [10, 20, 30],\n})\n\nps.refresh_view(df)\n</code></pre> <p>Then open:</p> <pre><code>http://127.0.0.1:8000\n</code></pre> <p><code>refresh_view()</code> publishes the object and starts the local plotsrv server automatically if one is not already running.</p> What happened? <p>plotsrv inspected the object, chose a renderer, stored the latest value in memory, and started the local browser UI if needed.</p> <p>Because the object was a DataFrame, plotsrv rendered it as a table.</p>","path":["plotsrv"],"tags":[]},{"location":"#what-plotsrv-can-show","level":2,"title":"What plotsrv can show","text":"<p>plotsrv chooses a renderer based on what you publish.</p> Object Renderer Polars or pandas DataFrame Table matplotlib or plotnine plot Plot <code>dict</code>, <code>list</code>, <code>tuple</code> JSON tree <code>str</code>, <code>bytes</code>, log output Text Markdown text or files Markdown HTML text or files HTML Image files Image Exceptions / tracebacks Traceback Other Python objects Python/repr view","path":["plotsrv"],"tags":[]},{"location":"#common-ways-to-use-plotsrv","level":2,"title":"Common ways to use plotsrv","text":"","path":["plotsrv"],"tags":[]},{"location":"#mark-views-in-a-project","level":3,"title":"Mark views in a project","text":"<p>Use <code>@ps.view(...)</code> to describe outputs in your code, then run a plotsrv server.</p> views.py<pre><code>import polars as pl\nimport plotsrv as ps\n\n@ps.view(label=\"Daily checks\", section=\"etl\")\ndef daily_checks():\n    return pl.DataFrame({\n        \"check\": [\"source rows\", \"loaded rows\", \"warnings\"],\n        \"value\": [10000, 9985, 15],\n    })\n</code></pre> <p>Then start plotsrv:</p> <pre><code>plotsrv run\n</code></pre> <p>This is the main server-style workflow: mark the views in code, start plotsrv, and let the UI organise them by section and label.</p>","path":["plotsrv"],"tags":[]},{"location":"#publish-from-python","level":3,"title":"Publish from Python","text":"<p>Use <code>refresh_view()</code> when working interactively in Python.</p> <pre><code>import polars as pl\nimport plotsrv as ps\n\ndf = pl.DataFrame({\n    \"name\": [\"alpha\", \"beta\", \"gamma\"],\n    \"value\": [10, 20, 30],\n})\n\nps.refresh_view(df, label=\"Example table\", section=\"quickstart\")\n</code></pre> <p>Use <code>publish_view()</code> when sending data to an already-running plotsrv server.</p> <pre><code>import polars as pl\nimport plotsrv as ps\n\ndf = pl.DataFrame({\n    \"name\": [\"alpha\", \"beta\", \"gamma\"],\n    \"value\": [10, 20, 30],\n})\n\nps.publish_view(df, label=\"Example table\", section=\"quickstart\")\n</code></pre>","path":["plotsrv"],"tags":[]},{"location":"#watch-files","level":3,"title":"Watch files","text":"<p>Use plotsrv to watch logs, CSV files, JSON files, markdown files, and other files on disk.</p> <pre><code>plotsrv watch app.log\n</code></pre> <pre><code>plotsrv watch results.csv\n</code></pre> <p>You can also start a plotsrv server and attach watched files at the same time:</p> <pre><code>plotsrv run --watch app.log --watch results.csv\n</code></pre>","path":["plotsrv"],"tags":[]},{"location":"#why-plotsrv","level":2,"title":"Why plotsrv?","text":"<p>plotsrv is for people who already have Python processes and want useful observability without building a full web app.</p> <p>It is designed to be:</p> <ul> <li>quick to start</li> <li>useful on headless servers</li> <li>friendly to scripts and ETL jobs</li> <li>good at rendering ordinary Python objects</li> <li>simple enough to use during exploration</li> <li>flexible enough to use in longer-running processes</li> </ul>","path":["plotsrv"],"tags":[]},{"location":"#what-plotsrv-is-not","level":2,"title":"What plotsrv is not","text":"<p>plotsrv is not trying to replace Grafana, Prometheus, Streamlit, Dash, Shiny, or a full BI platform.</p> <p>It is best thought of as a lightweight observability layer for Python work: a way to see the current state, recent outputs, logs, files, tables, and artifacts from a process while it runs.</p>","path":["plotsrv"],"tags":[]},{"location":"#next-steps","level":2,"title":"Next steps","text":"<p>Start with:</p> <ul> <li>Installation</li> <li>Quick start</li> <li>Ways to use plotsrv</li> <li>Renderer overview</li> </ul>","path":["plotsrv"],"tags":[]},{"location":"markdown/","level":1,"title":"Markdown in 5min","text":"","path":["Markdown in 5min"],"tags":[]},{"location":"markdown/#headers","level":2,"title":"Headers","text":"<pre><code># H1 Header\n## H2 Header\n### H3 Header\n#### H4 Header\n##### H5 Header\n###### H6 Header\n</code></pre>","path":["Markdown in 5min"],"tags":[]},{"location":"markdown/#text-formatting","level":2,"title":"Text formatting","text":"<pre><code>**bold text**\n*italic text*\n***bold and italic***\n~~strikethrough~~\n`inline code`\n</code></pre>","path":["Markdown in 5min"],"tags":[]},{"location":"markdown/#links-and-images","level":2,"title":"Links and images","text":"<pre><code>[Link text](https://example.com)\n[Link with title](https://example.com \"Hover title\")\n![Alt text](image.jpg)\n![Image with title](image.jpg \"Image title\")\n</code></pre>","path":["Markdown in 5min"],"tags":[]},{"location":"markdown/#lists","level":2,"title":"Lists","text":"<pre><code>Unordered:\n\n- Item 1\n- Item 2\n  - Nested item\n\nOrdered:\n\n1. First item\n2. Second item\n3. Third item\n</code></pre>","path":["Markdown in 5min"],"tags":[]},{"location":"markdown/#blockquotes","level":2,"title":"Blockquotes","text":"<pre><code>&gt; This is a blockquote\n&gt; Multiple lines\n&gt;&gt; Nested quote\n</code></pre>","path":["Markdown in 5min"],"tags":[]},{"location":"markdown/#code-blocks","level":2,"title":"Code blocks","text":"<pre><code>```javascript\nfunction hello() {\n  console.log(\"Hello, world!\");\n}\n```\n</code></pre>","path":["Markdown in 5min"],"tags":[]},{"location":"markdown/#tables","level":2,"title":"Tables","text":"<pre><code>| Header 1 | Header 2 | Header 3 |\n|----------|----------|----------|\n| Row 1    | Data     | Data     |\n| Row 2    | Data     | Data     |\n</code></pre>","path":["Markdown in 5min"],"tags":[]},{"location":"markdown/#horizontal-rule","level":2,"title":"Horizontal rule","text":"<pre><code>---\nor\n***\nor\n___\n</code></pre>","path":["Markdown in 5min"],"tags":[]},{"location":"markdown/#task-lists","level":2,"title":"Task lists","text":"<pre><code>- [x] Completed task\n- [ ] Incomplete task\n- [ ] Another task\n</code></pre>","path":["Markdown in 5min"],"tags":[]},{"location":"markdown/#escaping-characters","level":2,"title":"Escaping characters","text":"<pre><code>Use backslash to escape: \\* \\_ \\# \\`\n</code></pre>","path":["Markdown in 5min"],"tags":[]},{"location":"markdown/#line-breaks","level":2,"title":"Line breaks","text":"<pre><code>End a line with two spaces  \nto create a line break.\n\nOr use a blank line for a new paragraph.\n</code></pre>","path":["Markdown in 5min"],"tags":[]},{"location":"get-started/configuration-basics/","level":1,"title":"Configuration basics","text":"<p>plotsrv works without a config file.</p> <p>You can install it, run it, and publish objects immediately:</p> <pre><code>import polars as pl\nimport plotsrv as ps\n\ndf = pl.DataFrame({\n    \"name\": [\"alpha\", \"beta\", \"gamma\"],\n    \"value\": [10, 20, 30],\n})\n\nps.refresh_view(df)\n</code></pre> <p>A config file becomes useful when you want to control behaviour such as:</p> <ul> <li>table limits</li> <li>watched-file limits</li> <li>storage and historical snapshots</li> <li>freshness indicators</li> <li>UI customisation</li> <li>security-related options</li> <li>per-view settings</li> </ul>","path":["Get started","Configuration basics"],"tags":[]},{"location":"get-started/configuration-basics/#create-a-config-file","level":2,"title":"Create a config file","text":"<p>Create a starter config file with:</p> <pre><code>plotsrv config create\n</code></pre> <p>This creates:</p> <pre><code>plotsrv.yml\n</code></pre> <p>in the current directory.</p> <p>If the file already exists, plotsrv will not overwrite it unless you pass <code>--force</code>:</p> <pre><code>plotsrv config create --force\n</code></pre> <p>Warning</p> <p><code>--force</code> overwrites the existing config file.</p> <p>Use it carefully if you have already customised <code>plotsrv.yml</code>.</p>","path":["Get started","Configuration basics"],"tags":[]},{"location":"get-started/configuration-basics/#how-plotsrv-finds-config","level":2,"title":"How plotsrv finds config","text":"<p>By default, plotsrv looks for a config file in the current working directory:</p> <pre><code>plotsrv.yml\nplotsrv.yaml\n</code></pre> <p>You can also pass a config path explicitly.</p> <p>From the CLI:</p> <pre><code>plotsrv run --config path/to/plotsrv.yml\n</code></pre> <p>From Python:</p> <pre><code>import plotsrv as ps\n\nps.start_server(config=\"path/to/plotsrv.yml\")\n</code></pre> <p>You can also use the <code>PLOTSRV_CONFIG</code> environment variable.</p> <pre><code>PLOTSRV_CONFIG=path/to/plotsrv.yml plotsrv run\n</code></pre>","path":["Get started","Configuration basics"],"tags":[]},{"location":"get-started/configuration-basics/#a-small-config-file","level":2,"title":"A small config file","text":"<p>A minimal useful config might look like this:</p> plotsrv.yml<pre><code>limits:\n  watched_files:\n    max_bytes: 5000000\n\ntable-settings:\n  table_view_mode: rich\n  max_table_rows_rich: 1000\n\nstorage-settings:\n  enabled: false\n\nfreshness-settings:\n  enabled: false\n</code></pre> <p>You do not need to configure everything at once. Start with the settings you need.</p>","path":["Get started","Configuration basics"],"tags":[]},{"location":"get-started/configuration-basics/#table-settings","level":2,"title":"Table settings","text":"<p>Table settings control how many rows plotsrv shows in table views.</p> plotsrv.yml<pre><code>table-settings:\n  table_view_mode: rich\n  max_table_rows_simple: 200\n  max_table_rows_rich: 1000\n</code></pre> <p><code>table_view_mode</code> can be:</p> <ul> <li><code>rich</code></li> <li><code>simple</code></li> </ul> <p><code>rich</code> mode gives you the interactive table renderer with search, filters, column controls, pagination, and export.</p> <p><code>simple</code> mode renders a simpler static HTML table.</p> <p>Note</p> <p>Table display limits are there to keep the browser UI responsive.</p> <p>They do not mean your original Python object has been changed.</p>","path":["Get started","Configuration basics"],"tags":[]},{"location":"get-started/configuration-basics/#watched-file-limits","level":2,"title":"Watched-file limits","text":"<p>Watched-file limits control how much data plotsrv reads from files on disk.</p> plotsrv.yml<pre><code>limits:\n  watched_files:\n    max_bytes: 5000000\n</code></pre> <p>Use <code>off</code> to read the whole file:</p> plotsrv.yml<pre><code>limits:\n  watched_files:\n    max_bytes: off\n</code></pre> <p>This is useful for small files, but can be expensive for very large logs or CSV files.</p>","path":["Get started","Configuration basics"],"tags":[]},{"location":"get-started/configuration-basics/#renderer-limits","level":2,"title":"Renderer limits","text":"<p>Renderer limits control how much text-like content is displayed in the browser.</p> plotsrv.yml<pre><code>limits:\n  render:\n    text: 1000000\n    html: off\n    markdown: off\n</code></pre> <p>These settings apply to text-like renderers:</p> <ul> <li>text</li> <li>HTML</li> <li>markdown</li> </ul> <p>Use <code>off</code> to disable the render limit for that type.</p> <p>Note</p> <p>Watched-file limits and renderer limits are different.</p> <p>Watched-file limits control how much plotsrv reads from disk.</p> <p>Renderer limits control how much plotsrv displays in the browser.</p>","path":["Get started","Configuration basics"],"tags":[]},{"location":"get-started/configuration-basics/#table-publish-limits","level":2,"title":"Table publish limits","text":"<p>Table publish limits control how much table data plotsrv accepts and stores for rendering.</p> plotsrv.yml<pre><code>limits:\n  tables:\n    max_rows: 10000\n    max_columns: 200\n</code></pre> <p>These limits help protect the UI and server from very large table payloads.</p>","path":["Get started","Configuration basics"],"tags":[]},{"location":"get-started/configuration-basics/#storage-and-historical-snapshots","level":2,"title":"Storage and historical snapshots","text":"<p>Storage is disabled by default.</p> <p>Enable it if you want plotsrv to keep recent historical snapshots.</p> plotsrv.yml<pre><code>storage-settings:\n  enabled: true\n  root_dir: .plotsrv/store\n  default_keep_last: 5\n  max_snapshot_size_mb: 20\n</code></pre> <p>When storage is enabled, plotsrv can show previous versions of plots, tables, and artifacts through the history controls in the UI.</p> <p><code>default_keep_last</code> controls how many snapshots are kept per view.</p> plotsrv.yml<pre><code>storage-settings:\n  enabled: true\n  default_keep_last: 5\n</code></pre> <p>Use <code>default_keep_last: null</code> if you want to keep snapshots indefinitely.</p> <p>Warning</p> <p>Keeping snapshots indefinitely can use a lot of disk space.</p> <p>Prefer a small <code>default_keep_last</code> value unless you have a reason to keep more.</p>","path":["Get started","Configuration basics"],"tags":[]},{"location":"get-started/configuration-basics/#freshness-indicators","level":2,"title":"Freshness indicators","text":"<p>Freshness indicators are disabled by default.</p> <p>Enable them when a view is expected to update regularly.</p> plotsrv.yml<pre><code>freshness-settings:\n  enabled: true\n  expected_every: 60s\n  warn_after: 90s\n  overdue_after: 180s\n</code></pre> <p>This lets plotsrv show whether a view is:</p> <ul> <li>fresh</li> <li>stale</li> <li>overdue</li> </ul> <p>Freshness is useful for repeated jobs, scheduled processes, and monitoring-style workflows.</p>","path":["Get started","Configuration basics"],"tags":[]},{"location":"get-started/configuration-basics/#ui-customisation","level":2,"title":"UI customisation","text":"<p>You can customise the page title, header text, logo, favicon, and some UI controls.</p> plotsrv.yml<pre><code>ui-settings:\n  page_title: \"My plotsrv\"\n  header_text: \"Operations monitor\"\n  header_fill_colour: \"#ffffff\"\n  logo: \"logo.png\"\n  favicon: \"favicon.png\"\n</code></pre> <p>Logo and favicon paths are resolved relative to the config file.</p>","path":["Get started","Configuration basics"],"tags":[]},{"location":"get-started/configuration-basics/#security-related-settings","level":2,"title":"Security-related settings","text":"<p>Some routes and features are controlled by security settings.</p> plotsrv.yml<pre><code>security-settings:\n  docs_enabled: false\n  openapi_enabled: false\n  shutdown_enabled: false\n  control_local_only: true\n  tracebacks_enabled: false\n</code></pre> <p>For example:</p> <ul> <li><code>docs_enabled</code> controls FastAPI docs routes.</li> <li><code>openapi_enabled</code> controls the OpenAPI JSON route.</li> <li><code>shutdown_enabled</code> controls the browser-triggered shutdown endpoint.</li> <li><code>control_local_only</code> restricts control routes to local requests.</li> <li><code>tracebacks_enabled</code> controls whether traceback artifacts can be published and displayed.</li> </ul> <p>Warning</p> <p>Tracebacks can expose file paths and source-code context.</p> <p>They are disabled by default. Only enable them where that is acceptable.</p>","path":["Get started","Configuration basics"],"tags":[]},{"location":"get-started/configuration-basics/#named-config-instances","level":2,"title":"Named config instances","text":"<p>plotsrv supports named config instances.</p> <p>This is useful when the same config file should behave differently for different environments or processes.</p> plotsrv.yml<pre><code>table-settings:\n  default:\n    table_view_mode: rich\n    max_table_rows_rich: 1000\n\n  instances:\n    local:\n      max_table_rows_rich: 5000\n\n    production:\n      max_table_rows_rich: 1000\n</code></pre> <p>Run with a name:</p> <pre><code>plotsrv run --name local\n</code></pre> <p>Or from Python:</p> <pre><code>import plotsrv as ps\n\nps.start_server(name=\"local\")\n</code></pre> <p>The named instance is merged over the default settings for that section.</p>","path":["Get started","Configuration basics"],"tags":[]},{"location":"get-started/configuration-basics/#automatically-populate-per-view-config","level":2,"title":"Automatically populate per-view config","text":"<p>If you use <code>@ps.view(...)</code>, plotsrv can discover your views and populate parts of the config file.</p> <p>For freshness settings:</p> <pre><code>plotsrv config populate freshness .\n</code></pre> <p>For storage settings:</p> <pre><code>plotsrv config populate storage .\n</code></pre> <p>For render limits:</p> <pre><code>plotsrv config populate limits .\n</code></pre> <p>These commands scan for <code>@ps.view(...)</code> decorators and add per-view entries.</p> <p>For example:</p> views.py<pre><code>import plotsrv as ps\n\n@ps.view(label=\"Daily checks\", section=\"etl\")\ndef daily_checks():\n    return {\"status\": \"ok\"}\n</code></pre> <p>may produce a per-view config entry for:</p> <pre><code>etl:Daily checks\n</code></pre> <p>Note</p> <p>Automatic config population is useful once your project has several views.</p> <p>You can start without it and add it later.</p>","path":["Get started","Configuration basics"],"tags":[]},{"location":"get-started/configuration-basics/#config-from-python","level":2,"title":"Config from Python","text":"<p>Some runtime options can be supplied when starting plotsrv from Python.</p> <pre><code>import plotsrv as ps\n\nps.start_server(\n    config=\"plotsrv.yml\",\n    name=\"local\",\n    truncate=1000000,\n)\n</code></pre> <p>You can also disable text-like render truncation at runtime:</p> <pre><code>import plotsrv as ps\n\nps.start_server(no_truncate=True)\n</code></pre>","path":["Get started","Configuration basics"],"tags":[]},{"location":"get-started/configuration-basics/#config-from-the-cli","level":2,"title":"Config from the CLI","text":"<p>Common CLI config options include:</p> <pre><code>plotsrv run --config plotsrv.yml\n</code></pre> <pre><code>plotsrv run --name local\n</code></pre> <pre><code>plotsrv run --truncate 1000000\n</code></pre> <pre><code>plotsrv run --no-truncate\n</code></pre> <p>These runtime options are useful when you want to test a different config without editing the file.</p>","path":["Get started","Configuration basics"],"tags":[]},{"location":"get-started/configuration-basics/#recommended-path","level":2,"title":"Recommended path","text":"<p>Start simple:</p> <pre><code>plotsrv config create\n</code></pre> <p>Then edit only the section you need.</p> <p>A common progression is:</p> <ul> <li>adjust table or watched-file limits</li> <li>enable storage if you want historical snapshots</li> <li>enable freshness if views should update on a schedule</li> <li>customise the UI once the basic workflow is working</li> <li>use automatic config population when you have several <code>@ps.view(...)</code> views</li> </ul>","path":["Get started","Configuration basics"],"tags":[]},{"location":"get-started/configuration-basics/#next-steps","level":2,"title":"Next steps","text":"<ul> <li>Watch files</li> <li>Renderer overview</li> <li>Freshness indicators</li> <li>Historical snapshots</li> <li>Automatic config</li> </ul>","path":["Get started","Configuration basics"],"tags":[]},{"location":"get-started/installation/","level":1,"title":"Installation","text":"<p>Install plotsrv into the Python environment where your code runs.</p> pipuv <pre><code>pip install plotsrv\n</code></pre> <pre><code>uv add plotsrv\n</code></pre>","path":["Get started","Installation"],"tags":[]},{"location":"get-started/installation/#check-the-cli-is-available","level":2,"title":"Check the CLI is available","text":"<p>After installation, check that the <code>plotsrv</code> command is available:</p> <pre><code>plotsrv --help\n</code></pre> <p>You should see the plotsrv command-line help.</p>","path":["Get started","Installation"],"tags":[]},{"location":"get-started/installation/#check-the-python-api-is-available","level":2,"title":"Check the Python API is available","text":"<p>You can also check the Python import:</p> <pre><code>import plotsrv as ps\n\nprint(ps)\n</code></pre> <p>If that runs without an import error, plotsrv is available in your Python environment.</p>","path":["Get started","Installation"],"tags":[]},{"location":"get-started/installation/#recommended-first-check","level":2,"title":"Recommended first check","text":"<p>The fastest way to confirm everything is working is to publish a small table.</p> check_install.py<pre><code>import polars as pl\nimport plotsrv as ps\n\ndf = pl.DataFrame({\n    \"name\": [\"alpha\", \"beta\", \"gamma\"],\n    \"value\": [10, 20, 30],\n})\n\nps.refresh_view(df)\n</code></pre> <p>Then open:</p> <pre><code>http://127.0.0.1:8000\n</code></pre> <p>You should see the table in the plotsrv browser UI.</p> <p>Note</p> <p><code>ps.refresh_view()</code> starts a local plotsrv server automatically if one is not already running.</p>","path":["Get started","Installation"],"tags":[]},{"location":"get-started/installation/#installing-on-a-server","level":2,"title":"Installing on a server","text":"<p>plotsrv is often useful on a remote or headless server.</p> <p>Install it in the environment used by your script or project:</p> pipuv <pre><code>pip install plotsrv\n</code></pre> <pre><code>uv add plotsrv\n</code></pre> <p>Then run Python or the plotsrv CLI from that same environment.</p> <p>For example:</p> <pre><code>plotsrv run --host 127.0.0.1 --port 8000\n</code></pre> <p>If you are working over SSH, you can access the UI using port forwarding.</p> <pre><code>ssh -L 8000:127.0.0.1:8000 user@your-server\n</code></pre> <p>Then open this on your local machine:</p> <pre><code>http://127.0.0.1:8000\n</code></pre> <p>Warning</p> <p>Be careful before binding plotsrv to a public network interface.</p> <p>For many server workflows, an SSH tunnel is a safer first option than exposing the plotsrv server directly.</p>","path":["Get started","Installation"],"tags":[]},{"location":"get-started/installation/#next-step","level":2,"title":"Next step","text":"<p>Continue to the Quick start.</p>","path":["Get started","Installation"],"tags":[]},{"location":"get-started/interactive-use/","level":1,"title":"Use plotsrv interactively","text":"<p>plotsrv is useful when you are working in a Python session and want to inspect objects in a browser.</p> <p>This is especially helpful when you are:</p> <ul> <li>working over SSH</li> <li>using a headless server</li> <li>exploring data in a terminal session</li> <li>checking plots where rich local tooling is awkward</li> <li>inspecting tables, logs, or JSON-like objects repeatedly</li> </ul> <p>The simplest interactive workflow is:</p> <pre><code>ps.refresh_view(obj)\n</code></pre> <p><code>refresh_view()</code> publishes the object from the current Python process and starts a local plotsrv server automatically if one is not already running.</p>","path":["Get started","Use plotsrv interactively"],"tags":[]},{"location":"get-started/interactive-use/#start-with-a-table","level":2,"title":"Start with a table","text":"interactive_table.py<pre><code>import polars as pl\nimport plotsrv as ps\n\ndf = pl.DataFrame({\n    \"name\": [\"alpha\", \"beta\", \"gamma\"],\n    \"value\": [10, 20, 30],\n    \"group\": [\"A\", \"A\", \"B\"],\n})\n\nps.refresh_view(df)\n</code></pre> <p>Then open:</p> <pre><code>http://127.0.0.1:8000\n</code></pre> <p>You should see the table in the plotsrv UI.</p> <p>Note</p> <p>DataFrames are rendered as tables. The table view includes search, filters, column controls, pagination, and export.</p>","path":["Get started","Use plotsrv interactively"],"tags":[]},{"location":"get-started/interactive-use/#add-labels-and-sections","level":2,"title":"Add labels and sections","text":"<p>When you have more than one view, add a label and section.</p> interactive_labelled_table.py<pre><code>import polars as pl\nimport plotsrv as ps\n\ndf = pl.DataFrame({\n    \"name\": [\"alpha\", \"beta\", \"gamma\"],\n    \"value\": [10, 20, 30],\n    \"group\": [\"A\", \"A\", \"B\"],\n})\n\nps.refresh_view(df, label=\"Example table\", section=\"eda\")\n</code></pre> <ul> <li><code>label</code> is the display name for the view.</li> <li><code>section</code> groups related views together.</li> </ul> <p>This is useful when you are publishing several objects during an analysis.</p>","path":["Get started","Use plotsrv interactively"],"tags":[]},{"location":"get-started/interactive-use/#refresh-the-same-view","level":2,"title":"Refresh the same view","text":"<p>Calling <code>refresh_view()</code> again with the same label and section updates the same view.</p> interactive_refresh.py<pre><code>import polars as pl\nimport plotsrv as ps\n\ndf1 = pl.DataFrame({\n    \"name\": [\"alpha\", \"beta\", \"gamma\"],\n    \"value\": [10, 20, 30],\n})\n\nps.refresh_view(df1, label=\"Current data\", section=\"eda\")\n\ndf2 = pl.DataFrame({\n    \"name\": [\"alpha\", \"beta\", \"gamma\"],\n    \"value\": [12, 18, 35],\n})\n\nps.refresh_view(df2, label=\"Current data\", section=\"eda\")\n</code></pre> <p>The browser view updates to show the latest object.</p>","path":["Get started","Use plotsrv interactively"],"tags":[]},{"location":"get-started/interactive-use/#publish-a-plot","level":2,"title":"Publish a plot","text":"interactive_plot.py<pre><code>import matplotlib.pyplot as plt\nimport plotsrv as ps\n\nfig, ax = plt.subplots()\nax.plot([1, 2, 3, 4], [10, 20, 15, 30])\nax.set_title(\"Example metric\")\nax.set_xlabel(\"Run\")\nax.set_ylabel(\"Value\")\n\nps.refresh_view(fig, label=\"Metric plot\", section=\"eda\")\n</code></pre> <p>The plot renderer displays the figure as an image and provides an export button.</p>","path":["Get started","Use plotsrv interactively"],"tags":[]},{"location":"get-started/interactive-use/#publish-json-like-objects","level":2,"title":"Publish JSON-like objects","text":"<p>Dictionaries and lists are shown with the JSON renderer.</p> interactive_json.py<pre><code>import plotsrv as ps\n\nmetadata = {\n    \"experiment\": \"baseline-model\",\n    \"status\": \"complete\",\n    \"metrics\": {\n        \"accuracy\": 0.91,\n        \"precision\": 0.88,\n        \"recall\": 0.86,\n    },\n    \"features\": [\"age\", \"score\", \"previous_attempts\"],\n}\n\nps.refresh_view(metadata, label=\"Model metadata\", section=\"eda\")\n</code></pre> <p>The JSON renderer gives you an expandable tree, search controls, pinned values, and text view.</p>","path":["Get started","Use plotsrv interactively"],"tags":[]},{"location":"get-started/interactive-use/#publish-text-or-logs","level":2,"title":"Publish text or logs","text":"<p>Strings are shown with the text renderer.</p> interactive_text.py<pre><code>import plotsrv as ps\n\nlog_text = \"\"\"INFO started analysis\nINFO loaded 10000 rows\nWARNING 15 rows skipped\nINFO finished analysis\n\"\"\"\n\nps.refresh_view(log_text, label=\"Analysis log\", section=\"eda\")\n</code></pre> <p>The text renderer includes copy, wrap, reverse lines, and lightweight log styling.</p>","path":["Get started","Use plotsrv interactively"],"tags":[]},{"location":"get-started/interactive-use/#publish-a-generic-python-object","level":2,"title":"Publish a generic Python object","text":"<p>Objects that do not match a more specific renderer are shown using a Python/repr-style view.</p> interactive_python_object.py<pre><code>from dataclasses import dataclass\nimport plotsrv as ps\n\n@dataclass\nclass RunConfig:\n    model_name: str\n    threshold: float\n    max_rows: int\n\nconfig = RunConfig(\n    model_name=\"baseline\",\n    threshold=0.75,\n    max_rows=10000,\n)\n\nps.refresh_view(config, label=\"Run config\", section=\"eda\")\n</code></pre> <p>This can be useful when debugging object state.</p>","path":["Get started","Use plotsrv interactively"],"tags":[]},{"location":"get-started/interactive-use/#start-and-stop-the-server-explicitly","level":2,"title":"Start and stop the server explicitly","text":"<p>You usually do not need to start the server manually when using <code>refresh_view()</code>.</p> <p>But you can:</p> manual_server.py<pre><code>import plotsrv as ps\n\nps.start_server()\n</code></pre> <p>Then publish objects:</p> <pre><code>ps.refresh_view({\"status\": \"ok\"}, label=\"Status\", section=\"manual\")\n</code></pre> <p>Stop the server with:</p> <pre><code>ps.stop_server()\n</code></pre>","path":["Get started","Use plotsrv interactively"],"tags":[]},{"location":"get-started/interactive-use/#use-a-context-manager","level":2,"title":"Use a context manager","text":"<p>For scripts, you can use <code>plot_session()</code>:</p> plot_session_example.py<pre><code>import polars as pl\nimport plotsrv as ps\n\nwith ps.plot_session():\n    df = pl.DataFrame({\n        \"name\": [\"alpha\", \"beta\", \"gamma\"],\n        \"value\": [10, 20, 30],\n    })\n\n    ps.refresh_view(df, label=\"Session table\", section=\"example\")\n\n    input(\"Open http://127.0.0.1:8000, then press Enter to stop...\")\n</code></pre> <p>When the context exits, plotsrv stops the background server.</p>","path":["Get started","Use plotsrv interactively"],"tags":[]},{"location":"get-started/interactive-use/#working-over-ssh","level":2,"title":"Working over SSH","text":"<p>If plotsrv is running on a remote server, SSH port forwarding is often the simplest way to view it safely.</p> <p>On your local machine:</p> <pre><code>ssh -L 8000:127.0.0.1:8000 user@your-server\n</code></pre> <p>On the server, run your Python code as normal.</p> <p>Then open this on your local machine:</p> <pre><code>http://127.0.0.1:8000\n</code></pre> <p>Warning</p> <p>Avoid exposing plotsrv directly to the public internet unless you have thought through access controls, network boundaries, and deployment security.</p>","path":["Get started","Use plotsrv interactively"],"tags":[]},{"location":"get-started/interactive-use/#when-to-use-refresh_view-vs-publish_view","level":2,"title":"When to use <code>refresh_view()</code> vs <code>publish_view()</code>","text":"<p>Use <code>refresh_view()</code> when you are publishing from the same Python process that owns the in-process plotsrv server.</p> <pre><code>ps.refresh_view(df)\n</code></pre> <p>Use <code>publish_view()</code> when a plotsrv server is already running and your Python process should publish to it over HTTP.</p> <pre><code>ps.publish_view(df, label=\"Example table\", section=\"eda\")\n</code></pre> <p>For interactive use, <code>refresh_view()</code> is usually the simplest starting point.</p>","path":["Get started","Use plotsrv interactively"],"tags":[]},{"location":"get-started/interactive-use/#next-steps","level":2,"title":"Next steps","text":"<ul> <li>Run a plotsrv server</li> <li>Watch files</li> <li>Renderer overview</li> </ul>","path":["Get started","Use plotsrv interactively"],"tags":[]},{"location":"get-started/quick-start/","level":1,"title":"Quick start","text":"<p>This page shows the fastest way to see something in plotsrv.</p> <p>You will publish a small table from Python and view it in your browser.</p>","path":["Get started","Quick start"],"tags":[]},{"location":"get-started/quick-start/#publish-a-table","level":2,"title":"Publish a table","text":"<p>Start a Python session and run:</p> quickstart.py<pre><code>import polars as pl\nimport plotsrv as ps\n\ndf = pl.DataFrame({\n    \"name\": [\"alpha\", \"beta\", \"gamma\"],\n    \"value\": [10, 20, 30],\n})\n\nps.refresh_view(df)\n</code></pre> <p>Then open:</p> <pre><code>http://127.0.0.1:8000\n</code></pre> <p>You should see the DataFrame rendered as a table.</p> <p>Note</p> <p><code>ps.refresh_view()</code> is the quickest way to send an object to plotsrv from a Python session.</p> What happened? <p>plotsrv inspected the object, chose a renderer, stored the latest value in memory, and started the local browser UI if needed.</p> <p>Because the object was a DataFrame, plotsrv rendered it as a table.</p>","path":["Get started","Quick start"],"tags":[]},{"location":"get-started/quick-start/#add-a-label-and-section","level":2,"title":"Add a label and section","text":"<p>You can give the view a label and section.</p> labelled_table.py<pre><code>import polars as pl\nimport plotsrv as ps\n\ndf = pl.DataFrame({\n    \"name\": [\"alpha\", \"beta\", \"gamma\"],\n    \"value\": [10, 20, 30],\n})\n\nps.refresh_view(df, label=\"Example table\", section=\"quickstart\")\n</code></pre> <p>Labels and sections make the UI easier to navigate when you have more than one view.</p> <ul> <li><code>label</code> is the display name for the view.</li> <li><code>section</code> groups related views together.</li> <li>Together, they help organise the view selector in the browser UI.</li> </ul>","path":["Get started","Quick start"],"tags":[]},{"location":"get-started/quick-start/#publish-json","level":2,"title":"Publish JSON","text":"<p>Dictionaries and lists are shown as structured JSON views.</p> json_example.py<pre><code>import plotsrv as ps\n\nsummary = {\n    \"job\": \"daily-import\",\n    \"status\": \"ok\",\n    \"checks\": {\n        \"rows_in\": 10000,\n        \"rows_out\": 9985,\n        \"warnings\": [\"15 rows skipped\"],\n    },\n}\n\nps.refresh_view(summary, label=\"Job summary\", section=\"quickstart\")\n</code></pre> <p>The JSON renderer gives you an expandable tree view, search controls, and different viewing modes.</p>","path":["Get started","Quick start"],"tags":[]},{"location":"get-started/quick-start/#publish-text","level":2,"title":"Publish text","text":"<p>Strings are shown with the text renderer.</p> text_example.py<pre><code>import plotsrv as ps\n\nlog_text = \"\"\"INFO extract complete\nINFO transform complete\nWARNING 15 rows skipped\nINFO load complete\n\"\"\"\n\nps.refresh_view(log_text, label=\"Job log\", section=\"quickstart\")\n</code></pre> <p>The text renderer includes controls for copying, wrapping, reversing lines, and lightweight log styling.</p>","path":["Get started","Quick start"],"tags":[]},{"location":"get-started/quick-start/#publish-a-plot","level":2,"title":"Publish a plot","text":"<p>matplotlib figures are shown as plot views.</p> plot_example.py<pre><code>import matplotlib.pyplot as plt\nimport plotsrv as ps\n\nfig, ax = plt.subplots()\nax.plot([1, 2, 3], [10, 20, 15])\nax.set_title(\"Example metric\")\n\nps.refresh_view(fig, label=\"Metric plot\", section=\"quickstart\")\n</code></pre> <p>The plot renderer displays the figure as an image in the browser and includes an export button.</p>","path":["Get started","Quick start"],"tags":[]},{"location":"get-started/quick-start/#publish-more-than-one-view","level":2,"title":"Publish more than one view","text":"<p>You can publish several views into different labels and sections.</p> multiple_views.py<pre><code>import polars as pl\nimport plotsrv as ps\n\nchecks = pl.DataFrame({\n    \"check\": [\"source rows\", \"loaded rows\", \"warnings\"],\n    \"value\": [10000, 9985, 15],\n})\n\nsummary = {\n    \"job\": \"daily-import\",\n    \"status\": \"warning\",\n    \"message\": \"15 rows were skipped\",\n}\n\nlog_text = \"\"\"INFO extract complete\nINFO transform complete\nWARNING 15 rows skipped\nINFO load complete\n\"\"\"\n\nps.refresh_view(checks, label=\"Checks\", section=\"etl\")\nps.refresh_view(summary, label=\"Summary\", section=\"etl\")\nps.refresh_view(log_text, label=\"Log\", section=\"etl\")\n</code></pre> <p>The browser UI will show these as separate views in the view selector.</p>","path":["Get started","Quick start"],"tags":[]},{"location":"get-started/quick-start/#stop-the-server","level":2,"title":"Stop the server","text":"<p>If you started plotsrv from Python, you can stop it with:</p> <pre><code>import plotsrv as ps\n\nps.stop_server()\n</code></pre> <p>If plotsrv was started from the command line, stop it from the terminal with <code>Ctrl+C</code>.</p>","path":["Get started","Quick start"],"tags":[]},{"location":"get-started/quick-start/#next-steps","level":2,"title":"Next steps","text":"<p>Once the quick start works, read:</p> <ul> <li>Ways to use plotsrv</li> <li>Run a plotsrv server</li> <li>Use plotsrv interactively</li> <li>Watch files</li> </ul>","path":["Get started","Quick start"],"tags":[]},{"location":"get-started/run-a-plotsrv-server/","level":1,"title":"Run a plotsrv server","text":"<p>This is the main server-style way to use plotsrv.</p> <p>You mark useful outputs in your Python code with <code>@ps.view(...)</code>, then start the plotsrv server with:</p> <pre><code>plotsrv run\n</code></pre> <p>plotsrv discovers the marked views and shows them in the browser UI.</p> <p>Note</p> <p>In this workflow, <code>@ps.view(...)</code> is the main way to describe what should appear in the plotsrv UI.</p> <p><code>publish_view()</code> is then used by running Python code to send data into those views.</p>","path":["Get started","Run a plotsrv server"],"tags":[]},{"location":"get-started/run-a-plotsrv-server/#a-minimal-view","level":2,"title":"A minimal view","text":"<p>Create a Python file:</p> views.py<pre><code>import polars as pl\nimport plotsrv as ps\n\n@ps.view(label=\"Daily checks\", section=\"etl\")\ndef daily_checks():\n    return pl.DataFrame({\n        \"check\": [\"source rows\", \"loaded rows\", \"warnings\"],\n        \"value\": [10000, 9985, 15],\n    })\n</code></pre> <p>Then run plotsrv from the same project directory:</p> <pre><code>plotsrv run\n</code></pre> <p>Open:</p> <pre><code>http://127.0.0.1:8000\n</code></pre> <p>You should see a plotsrv UI with a view called Daily checks in the etl section.</p> What did plotsrv discover? <p>plotsrv scans Python files for <code>@ps.view(...)</code> / <code>@view(...)</code> decorators.</p> <p>It can use that metadata to pre-register views in the UI without needing to execute your functions just to build the view selector.</p>","path":["Get started","Run a plotsrv server"],"tags":[]},{"location":"get-started/run-a-plotsrv-server/#publishing-data-to-the-view","level":2,"title":"Publishing data to the view","text":"<p>The decorator gives plotsrv the view metadata.</p> <p>To send data to a running plotsrv server, publish an object with the same label and section.</p> publish_daily_checks.py<pre><code>import polars as pl\nimport plotsrv as ps\n\ndf = pl.DataFrame({\n    \"check\": [\"source rows\", \"loaded rows\", \"warnings\"],\n    \"value\": [10000, 9985, 15],\n})\n\nps.publish_view(df, label=\"Daily checks\", section=\"etl\")\n</code></pre> <p>The browser UI will update the Daily checks view.</p>","path":["Get started","Run a plotsrv server"],"tags":[]},{"location":"get-started/run-a-plotsrv-server/#why-use-both-view-and-publish_view","level":2,"title":"Why use both <code>@view</code> and <code>publish_view()</code>?","text":"<p>They do different jobs.</p> <p><code>@ps.view(...)</code> describes the view:</p> <pre><code>@ps.view(label=\"Daily checks\", section=\"etl\")\ndef daily_checks():\n    ...\n</code></pre> <p><code>ps.publish_view(...)</code> sends data to the server:</p> <pre><code>ps.publish_view(df, label=\"Daily checks\", section=\"etl\")\n</code></pre> <p>For small examples, these may look repetitive. In a real project, the decorator helps plotsrv discover and organise views, while <code>publish_view()</code> can be called from the process that produces the actual output.</p>","path":["Get started","Run a plotsrv server"],"tags":[]},{"location":"get-started/run-a-plotsrv-server/#using-a-callable","level":2,"title":"Using a callable","text":"<p>You can also point plotsrv at a callable target.</p> <p>For example, if you have:</p> my_project/views.py<pre><code>import polars as pl\nimport plotsrv as ps\n\n@ps.view(label=\"Summary\", section=\"etl\")\ndef summary():\n    return pl.DataFrame({\n        \"metric\": [\"rows_in\", \"rows_out\", \"warnings\"],\n        \"value\": [10000, 9985, 15],\n    })\n</code></pre> <p>You can run plotsrv in callable mode:</p> <pre><code>plotsrv run my_project.views:summary --mode callable --keep-alive\n</code></pre> <p>This runs the callable and keeps the server alive.</p> <p>You can also run it repeatedly:</p> <pre><code>plotsrv run my_project.views:summary --mode callable --call-every 60\n</code></pre> <p>Note</p> <p>Callable mode currently expects functions that can be called without required arguments.</p>","path":["Get started","Run a plotsrv server"],"tags":[]},{"location":"get-started/run-a-plotsrv-server/#running-against-a-project-path","level":2,"title":"Running against a project path","text":"<p>You can also give <code>plotsrv run</code> an explicit path to scan:</p> <pre><code>plotsrv run ./src\n</code></pre> <p>or:</p> <pre><code>plotsrv run .\n</code></pre> <p>If no target is supplied, plotsrv tries to detect a Python project root from the current directory.</p>","path":["Get started","Run a plotsrv server"],"tags":[]},{"location":"get-started/run-a-plotsrv-server/#labels-and-sections","level":2,"title":"Labels and sections","text":"<p>Labels and sections control how views are shown in the UI.</p> <pre><code>@ps.view(label=\"Bookings\", section=\"operations\")\ndef bookings():\n    ...\n</code></pre> <ul> <li><code>label</code> is the display name for the view.</li> <li><code>section</code> groups related views together.</li> <li>Together they form a natural view identity.</li> </ul> <p>For example:</p> <pre><code>ps.publish_view(df, label=\"Bookings\", section=\"operations\")\n</code></pre> <p>will publish to the same Bookings view in the operations section.</p>","path":["Get started","Run a plotsrv server"],"tags":[]},{"location":"get-started/run-a-plotsrv-server/#publishing-different-object-types","level":2,"title":"Publishing different object types","text":"<p><code>publish_view()</code> can publish many object types.</p>","path":["Get started","Run a plotsrv server"],"tags":[]},{"location":"get-started/run-a-plotsrv-server/#table","level":3,"title":"Table","text":"<pre><code>import polars as pl\nimport plotsrv as ps\n\ndf = pl.DataFrame({\n    \"centre\": [\"A\", \"B\", \"C\"],\n    \"returned\": [120, 98, 143],\n})\n\nps.publish_view(df, label=\"Returns\", section=\"operations\")\n</code></pre>","path":["Get started","Run a plotsrv server"],"tags":[]},{"location":"get-started/run-a-plotsrv-server/#json","level":3,"title":"JSON","text":"<pre><code>import plotsrv as ps\n\nsummary = {\n    \"job\": \"daily-import\",\n    \"status\": \"ok\",\n    \"rows_loaded\": 9985,\n    \"warnings\": 15,\n}\n\nps.publish_view(summary, label=\"Summary\", section=\"operations\")\n</code></pre>","path":["Get started","Run a plotsrv server"],"tags":[]},{"location":"get-started/run-a-plotsrv-server/#text","level":3,"title":"Text","text":"<pre><code>import plotsrv as ps\n\nlog_text = \"\"\"INFO job started\nINFO extract complete\nWARNING 15 rows skipped\nINFO job finished\n\"\"\"\n\nps.publish_view(log_text, label=\"Log\", section=\"operations\")\n</code></pre>","path":["Get started","Run a plotsrv server"],"tags":[]},{"location":"get-started/run-a-plotsrv-server/#plot","level":3,"title":"Plot","text":"<pre><code>import matplotlib.pyplot as plt\nimport plotsrv as ps\n\nfig, ax = plt.subplots()\nax.plot([1, 2, 3], [120, 98, 143])\nax.set_title(\"Returned scripts\")\n\nps.publish_view(fig, label=\"Returns plot\", section=\"operations\")\n</code></pre>","path":["Get started","Run a plotsrv server"],"tags":[]},{"location":"get-started/run-a-plotsrv-server/#host-and-port","level":2,"title":"Host and port","text":"<p>By default, plotsrv runs on:</p> <pre><code>http://127.0.0.1:8000\n</code></pre> <p>You can choose a different host or port:</p> <pre><code>plotsrv run --host 127.0.0.1 --port 9000\n</code></pre> <p>Then publish to that port:</p> <pre><code>import plotsrv as ps\n\nps.publish_view(\n    {\"status\": \"ok\"},\n    label=\"Status\",\n    section=\"example\",\n    host=\"127.0.0.1\",\n    port=9000,\n)\n</code></pre> <p>Warning</p> <p>Be careful before binding plotsrv to <code>0.0.0.0</code> or exposing it outside your machine.</p> <p>For many server workflows, an SSH tunnel or reverse proxy with appropriate access controls is safer than exposing plotsrv directly.</p>","path":["Get started","Run a plotsrv server"],"tags":[]},{"location":"get-started/run-a-plotsrv-server/#watching-files-while-the-server-runs","level":2,"title":"Watching files while the server runs","text":"<p>You can attach watched files when starting the server:</p> <pre><code>plotsrv run --watch app.log --watch results.csv\n</code></pre> <p>This is useful when your process already writes logs or output files to disk.</p> <p>Read more: Watch files.</p>","path":["Get started","Run a plotsrv server"],"tags":[]},{"location":"get-started/run-a-plotsrv-server/#stop-the-server","level":2,"title":"Stop the server","text":"<p>If plotsrv is running in the terminal, stop it with:</p> <pre><code>Ctrl+C\n</code></pre> <p>If you started it from Python with <code>ps.start_server()</code>, stop it with:</p> <pre><code>import plotsrv as ps\n\nps.stop_server()\n</code></pre>","path":["Get started","Run a plotsrv server"],"tags":[]},{"location":"get-started/run-a-plotsrv-server/#next-steps","level":2,"title":"Next steps","text":"<ul> <li>Use plotsrv interactively</li> <li>Watch files</li> <li>Configuration basics</li> <li>View labels and sections</li> </ul>","path":["Get started","Run a plotsrv server"],"tags":[]},{"location":"get-started/watch-files/","level":1,"title":"Watch files","text":"<p>plotsrv can watch files on disk and publish them into the browser UI.</p> <p>This is useful when your process already writes logs, CSV files, JSON files, markdown reports, HTML reports, images, or other output files.</p> <p>The simplest command is:</p> <pre><code>plotsrv watch app.log\n</code></pre> <p>Then open:</p> <pre><code>http://127.0.0.1:8000\n</code></pre> <p>plotsrv will watch the file and update the browser view when it changes.</p>","path":["Get started","Watch files"],"tags":[]},{"location":"get-started/watch-files/#watch-a-log-file","level":2,"title":"Watch a log file","text":"<pre><code>plotsrv watch app.log\n</code></pre> <p>Text-like files are shown with the text renderer.</p> <p>The text renderer includes:</p> <ul> <li>copy</li> <li>word wrap</li> <li>reverse line order</li> <li>lightweight log colouring</li> <li>jump to bottom</li> </ul> <p>By default, log-like files are read from the tail of the file, so you see the newest output first.</p>","path":["Get started","Watch files"],"tags":[]},{"location":"get-started/watch-files/#watch-a-csv-file","level":2,"title":"Watch a CSV file","text":"<pre><code>plotsrv watch results.csv\n</code></pre> <p>CSV files are shown as tables.</p> <p>The table renderer includes:</p> <ul> <li>search</li> <li>filters</li> <li>column visibility controls</li> <li>pagination</li> <li>export</li> </ul> <p>When tailing CSV files, plotsrv keeps the header row so the table can still be parsed correctly.</p>","path":["Get started","Watch files"],"tags":[]},{"location":"get-started/watch-files/#watch-a-json-file","level":2,"title":"Watch a JSON file","text":"<pre><code>plotsrv watch status.json\n</code></pre> <p>JSON files are shown with the JSON renderer.</p> <p>For example, a file like this:</p> status.json<pre><code>{\n  \"job\": \"daily-import\",\n  \"status\": \"ok\",\n  \"rows_loaded\": 9985,\n  \"warnings\": 15\n}\n</code></pre> <p>will be rendered as a structured JSON tree.</p>","path":["Get started","Watch files"],"tags":[]},{"location":"get-started/watch-files/#watch-multiple-files","level":2,"title":"Watch multiple files","text":"<p>You can attach watched files when starting a plotsrv server:</p> <pre><code>plotsrv run --watch app.log --watch results.csv --watch status.json\n</code></pre> <p>This gives you one browser UI with multiple views.</p>","path":["Get started","Watch files"],"tags":[]},{"location":"get-started/watch-files/#add-labels","level":2,"title":"Add labels","text":"<p>By default, watched files use the filename as the label.</p> <p>You can provide labels explicitly:</p> <pre><code>plotsrv run \\\n  --watch app.log \\\n  --watch results.csv \\\n  --watch-label \"Application log\" \\\n  --watch-label \"Results table\"\n</code></pre> <p>The labels are matched to the watched files in order.</p>","path":["Get started","Watch files"],"tags":[]},{"location":"get-started/watch-files/#add-sections","level":2,"title":"Add sections","text":"<p>Watched files default to the <code>watch</code> section.</p> <p>You can provide a section:</p> <pre><code>plotsrv run \\\n  --watch app.log \\\n  --watch results.csv \\\n  --watch-section \"files\"\n</code></pre> <p>A single <code>--watch-section</code> applies to all watched files.</p> <p>You can also provide one section per watched file:</p> <pre><code>plotsrv run \\\n  --watch app.log \\\n  --watch results.csv \\\n  --watch-section \"logs\" \\\n  --watch-section \"tables\"\n</code></pre>","path":["Get started","Watch files"],"tags":[]},{"location":"get-started/watch-files/#choose-head-or-tail-mode","level":2,"title":"Choose head or tail mode","text":"<p>plotsrv chooses a sensible default read mode based on the file type.</p> <ul> <li>logs and unknown text files default to <code>tail</code></li> <li>CSV, JSON, YAML, TOML, INI, markdown, HTML, and image files default to <code>head</code></li> </ul> <p>You can override this.</p> <p>For one watched file:</p> <pre><code>plotsrv watch app.log --head\n</code></pre> <pre><code>plotsrv watch app.log --tail\n</code></pre> <p>When using <code>plotsrv run --watch</code>, use <code>--watch-head</code> or <code>--watch-tail</code>:</p> <pre><code>plotsrv run --watch app.log --watch-tail\n</code></pre> <pre><code>plotsrv run --watch config.json --watch-head\n</code></pre>","path":["Get started","Watch files"],"tags":[]},{"location":"get-started/watch-files/#limit-how-much-of-a-file-is-read","level":2,"title":"Limit how much of a file is read","text":"<p>Large files can be limited with <code>--max-bytes</code>:</p> <pre><code>plotsrv watch app.log --max-bytes 5000000\n</code></pre> <p>Use <code>off</code> to read the whole file:</p> <pre><code>plotsrv watch app.log --max-bytes off\n</code></pre> <p>When using <code>plotsrv run --watch</code>, use <code>--watch-max-bytes</code>:</p> <pre><code>plotsrv run --watch app.log --watch-max-bytes 5000000\n</code></pre> <p>The default value can also be set in config:</p> plotsrv.yml<pre><code>limits:\n  watched_files:\n    max_bytes: 5000000\n</code></pre> <p>Note</p> <p>The watched-file byte limit controls how much data plotsrv reads from disk.</p> <p>Renderer limits control how much text-like content is displayed in the browser.</p>","path":["Get started","Watch files"],"tags":[]},{"location":"get-started/watch-files/#choose-file-interpretation","level":2,"title":"Choose file interpretation","text":"<p>By default, plotsrv infers the file type from the extension.</p> <p>You can force text mode:</p> <pre><code>plotsrv watch output.data --kind text\n</code></pre> <p>Or force JSON mode:</p> <pre><code>plotsrv watch output.data --kind json\n</code></pre> <p>For <code>plotsrv run --watch</code>, use <code>--watch-kind</code>:</p> <pre><code>plotsrv run --watch output.data --watch-kind text\n</code></pre> <p>Supported watch kinds are:</p> <ul> <li><code>auto</code></li> <li><code>text</code></li> <li><code>json</code></li> </ul>","path":["Get started","Watch files"],"tags":[]},{"location":"get-started/watch-files/#use-watches-from-python","level":2,"title":"Use watches from Python","text":"<p>You can also start watches from the Python API.</p> watch_from_python.py<pre><code>import plotsrv as ps\n\nps.start_server(\n    watches=[\n        ps.WatchConfig(\n            path=\"app.log\",\n            label=\"Application log\",\n            section=\"files\",\n            kind=\"text\",\n        ),\n        ps.WatchConfig(\n            path=\"results.csv\",\n            label=\"Results table\",\n            section=\"files\",\n        ),\n    ],\n)\n\ninput(\"Open http://127.0.0.1:8000, then press Enter to stop...\")\nps.stop_server()\n</code></pre>","path":["Get started","Watch files"],"tags":[]},{"location":"get-started/watch-files/#what-file-types-can-plotsrv-infer","level":2,"title":"What file types can plotsrv infer?","text":"<p>plotsrv can infer common file types from file extensions.</p> Extension Renderer <code>.csv</code> Table <code>.json</code> JSON <code>.yaml</code>, <code>.yml</code> JSON <code>.toml</code> JSON <code>.ini</code>, <code>.cfg</code> JSON <code>.md</code>, <code>.markdown</code> Markdown <code>.html</code>, <code>.htm</code> HTML <code>.png</code>, <code>.jpg</code>, <code>.jpeg</code>, <code>.gif</code>, <code>.webp</code>, <code>.bmp</code>, <code>.svg</code> Image anything else Text","path":["Get started","Watch files"],"tags":[]},{"location":"get-started/watch-files/#watch-mode-vs-publishing-from-python","level":2,"title":"Watch mode vs publishing from Python","text":"<p>Watching files is best when your process already writes useful output to disk.</p> <p>Publishing from Python is better when the useful object already exists in memory.</p> <p>For example:</p> <pre><code>ps.publish_view(df, label=\"Results\", section=\"etl\")\n</code></pre> <p>is better than writing a temporary CSV just so plotsrv can watch it.</p> <p>But if your process already writes <code>results.csv</code>, then watching the file is a simple way to make it visible.</p>","path":["Get started","Watch files"],"tags":[]},{"location":"get-started/watch-files/#next-steps","level":2,"title":"Next steps","text":"<ul> <li>Configuration basics</li> <li>Renderer overview</li> <li>Run a plotsrv server</li> </ul>","path":["Get started","Watch files"],"tags":[]},{"location":"get-started/ways-to-use/","level":1,"title":"Ways to use plotsrv","text":"<p>plotsrv can be used in a few different ways.</p> <p>If you are unsure where to start, start with this:</p> <pre><code>plotsrv run\n</code></pre> <p>Then add <code>@ps.view(...)</code> decorators to the Python functions you want plotsrv to discover.</p> views.py<pre><code>import polars as pl\nimport plotsrv as ps\n\n@ps.view(label=\"Daily checks\", section=\"etl\")\ndef daily_checks():\n    return pl.DataFrame({\n        \"check\": [\"source rows\", \"loaded rows\", \"warnings\"],\n        \"value\": [10000, 9985, 15],\n    })\n</code></pre> <p>This is the main server-style workflow: mark useful outputs in your code, run a plotsrv server, and publish data into those views as your process runs.</p>","path":["Get started","Ways to use plotsrv"],"tags":[]},{"location":"get-started/ways-to-use/#choose-a-workflow","level":2,"title":"Choose a workflow","text":"You want to... Start with Mark useful outputs in a project <code>@ps.view(...)</code> and <code>plotsrv run</code> Send an object from a Python session <code>ps.refresh_view(obj)</code> Send an object to an already-running plotsrv server <code>ps.publish_view(obj, label=...)</code> Watch a file on disk <code>plotsrv watch file.log</code> Watch files while running a plotsrv server <code>plotsrv run --watch file.log</code> Keep historical snapshots enable <code>storage-settings</code> Show freshness/staleness indicators enable <code>freshness-settings</code> <p>Note</p> <p>You do not need to choose perfectly up front.</p> <p>Most projects start with one simple workflow, then gradually add labels, sections, watched files, storage, or freshness indicators later.</p>","path":["Get started","Ways to use plotsrv"],"tags":[]},{"location":"get-started/ways-to-use/#1-run-a-plotsrv-server","level":2,"title":"1. Run a plotsrv server","text":"<p>Use this when you have a Python project, script, package, or process that you want to observe from a browser UI.</p> <p>Mark a view in your code:</p> views.py<pre><code>import polars as pl\nimport plotsrv as ps\n\n@ps.view(label=\"Current status\", section=\"etl\")\ndef current_status():\n    return {\n        \"job\": \"daily-import\",\n        \"status\": \"ok\",\n        \"rows_loaded\": 9985,\n    }\n</code></pre> <p>Start the server:</p> <pre><code>plotsrv run\n</code></pre> <p>Then publish to the same view from Python:</p> <pre><code>import plotsrv as ps\n\nps.publish_view(\n    {\n        \"job\": \"daily-import\",\n        \"status\": \"ok\",\n        \"rows_loaded\": 9985,\n    },\n    label=\"Current status\",\n    section=\"etl\",\n)\n</code></pre> <p>The decorator gives plotsrv metadata about the view. <code>publish_view()</code> sends data to the running plotsrv server.</p> <p>Read more: Run a plotsrv server.</p>","path":["Get started","Ways to use plotsrv"],"tags":[]},{"location":"get-started/ways-to-use/#2-use-plotsrv-interactively","level":2,"title":"2. Use plotsrv interactively","text":"<p>Use this when you are working in a Python session and want to quickly inspect an object.</p> interactive.py<pre><code>import polars as pl\nimport plotsrv as ps\n\ndf = pl.DataFrame({\n    \"name\": [\"alpha\", \"beta\", \"gamma\"],\n    \"value\": [10, 20, 30],\n})\n\nps.refresh_view(df, label=\"Example table\", section=\"interactive\")\n</code></pre> <p>Open:</p> <pre><code>http://127.0.0.1:8000\n</code></pre> <p><code>refresh_view()</code> is the simplest way to publish an object from the same Python process.</p> <p>It is useful for:</p> <ul> <li>exploratory data analysis</li> <li>working over SSH</li> <li>inspecting large-ish tables in a browser</li> <li>checking plots on a headless server</li> <li>viewing JSON-like objects without printing them to the terminal</li> </ul> <p>Read more: Use plotsrv interactively.</p>","path":["Get started","Ways to use plotsrv"],"tags":[]},{"location":"get-started/ways-to-use/#3-watch-files","level":2,"title":"3. Watch files","text":"<p>Use this when your process already writes useful files to disk.</p> <p>For example, watch a log file:</p> <pre><code>plotsrv watch app.log\n</code></pre> <p>Watch a CSV file as a table:</p> <pre><code>plotsrv watch results.csv\n</code></pre> <p>Or start a server and attach watched files:</p> <pre><code>plotsrv run --watch app.log --watch results.csv\n</code></pre> <p>plotsrv can infer common file types, including text, logs, CSV, JSON, YAML, TOML, markdown, HTML, and images.</p> <p>Read more: Watch files.</p>","path":["Get started","Ways to use plotsrv"],"tags":[]},{"location":"get-started/ways-to-use/#4-add-configuration","level":2,"title":"4. Add configuration","text":"<p>Once you have more than one view, create a config file:</p> <pre><code>plotsrv config create\n</code></pre> <p>Configuration lets you control things like:</p> <ul> <li>table limits</li> <li>watched-file byte limits</li> <li>storage</li> <li>historical snapshots</li> <li>freshness indicators</li> <li>UI customisation</li> <li>security settings</li> </ul> <p>Read more: Configuration basics.</p>","path":["Get started","Ways to use plotsrv"],"tags":[]},{"location":"get-started/ways-to-use/#5-add-storage-and-freshness-later","level":2,"title":"5. Add storage and freshness later","text":"<p>You do not need storage or freshness indicators to start using plotsrv.</p> <p>They are useful once plotsrv is observing a repeated or scheduled process.</p> <p>Storage lets you keep recent snapshots:</p> plotsrv.yml<pre><code>storage-settings:\n  enabled: true\n  default_keep_last: 5\n</code></pre> <p>Freshness indicators let the UI show whether a view is fresh, stale, or overdue:</p> plotsrv.yml<pre><code>freshness-settings:\n  enabled: true\n  expected_every: 60s\n  warn_after: 90s\n  overdue_after: 180s\n</code></pre> <p>These features are best added after the basic publishing workflow is working.</p>","path":["Get started","Ways to use plotsrv"],"tags":[]},{"location":"get-started/ways-to-use/#recommended-path","level":2,"title":"Recommended path","text":"<p>For most users:</p> <ul> <li>start with Quick start</li> <li>then try Run a plotsrv server</li> <li>then add Watch files if your process writes files</li> <li>then add Configuration basics once you have multiple views</li> </ul>","path":["Get started","Ways to use plotsrv"],"tags":[]},{"location":"get-started/what-is-plotsrv/","level":1,"title":"What is plotsrv?","text":"<p>plotsrv is a lightweight browser UI for observing live outputs from Python processes.</p> <p>You publish something from Python, and plotsrv shows it in a browser.</p> <p>That “something” might be:</p> <ul> <li>a table</li> <li>a plot</li> <li>a log</li> <li>a JSON object</li> <li>a markdown report</li> <li>an HTML report</li> <li>an image</li> <li>a traceback</li> <li>a generic Python object</li> </ul> <p>Note</p> <p>plotsrv is not mainly for building dashboards. It is for observing what Python processes are producing while they run.</p>","path":["Get started","What is plotsrv?"],"tags":[]},{"location":"get-started/what-is-plotsrv/#the-basic-idea","level":2,"title":"The basic idea","text":"<p>A Python process sends data to plotsrv.</p> <p>plotsrv stores the latest version of that data in memory and renders it in a browser UI.</p> <p>For example:</p> example.py<pre><code>import polars as pl\nimport plotsrv as ps\n\ndf = pl.DataFrame({\n    \"name\": [\"alpha\", \"beta\", \"gamma\"],\n    \"value\": [10, 20, 30],\n})\n\nps.refresh_view(df)\n</code></pre> <p>Open:</p> <pre><code>http://127.0.0.1:8000\n</code></pre> <p>You should see the DataFrame as an interactive table.</p> What happened? <p><code>ps.refresh_view(df)</code> inspected the object, recognised it as a DataFrame, published it into plotsrv, and started the local browser UI if needed.</p> <p>The current object is held in memory by the running plotsrv server.</p>","path":["Get started","What is plotsrv?"],"tags":[]},{"location":"get-started/what-is-plotsrv/#why-use-it","level":2,"title":"Why use it?","text":"<p>plotsrv is useful when you want to see what a Python process is doing without building a dashboard application.</p> <p>Common examples include:</p> <ul> <li>monitoring an ETL job</li> <li>checking the latest output from a scheduled script</li> <li>inspecting logs from a running process</li> <li>viewing a table on a headless server</li> <li>exploring JSON-like Python objects</li> <li>publishing plots during experimentation</li> <li>checking the state of an ML or AI inference process</li> <li>seeing tracebacks in a browser UI</li> </ul>","path":["Get started","What is plotsrv?"],"tags":[]},{"location":"get-started/what-is-plotsrv/#how-plotsrv-is-different-from-a-dashboard-framework","level":2,"title":"How plotsrv is different from a dashboard framework","text":"<p>With a dashboard framework, you usually start by designing an application.</p> <p>With plotsrv, you usually start by publishing an object:</p> <pre><code>ps.refresh_view(obj)\n</code></pre> <p>or by marking a function as a view:</p> <pre><code>import plotsrv as ps\n\n@ps.view(label=\"Daily result\", section=\"etl\")\ndef daily_result():\n    return {\n        \"status\": \"ok\",\n        \"rows\": 10000,\n        \"warnings\": 0,\n    }\n</code></pre> <p>plotsrv then chooses a suitable renderer.</p> <p>That means the first useful version of a plotsrv view can be very small.</p>","path":["Get started","What is plotsrv?"],"tags":[]},{"location":"get-started/what-is-plotsrv/#where-plotsrv-fits-well","level":2,"title":"Where plotsrv fits well","text":"<p>plotsrv works well for:</p> <ul> <li>scripts</li> <li>ETL pipelines</li> <li>scheduled jobs</li> <li>development servers</li> <li>exploratory analysis</li> <li>headless machines</li> <li>SSH workflows</li> <li>operational debugging</li> <li>lightweight monitoring</li> </ul> <p>It is especially useful when the output is already available in Python, but you want a better way to inspect it than printing to the terminal.</p>","path":["Get started","What is plotsrv?"],"tags":[]},{"location":"get-started/what-is-plotsrv/#where-plotsrv-may-not-be-the-right-tool","level":2,"title":"Where plotsrv may not be the right tool","text":"<p>plotsrv may not be the right tool if you need:</p> <ul> <li>a polished multi-user dashboard</li> <li>a public internet-facing application</li> <li>full authentication and user management</li> <li>a replacement for Grafana or Prometheus</li> <li>a business intelligence platform</li> <li>a complex custom frontend</li> </ul> <p>You can still deploy plotsrv carefully, but its core purpose is simple: observe what Python processes are producing.</p>","path":["Get started","What is plotsrv?"],"tags":[]},{"location":"get-started/what-is-plotsrv/#the-shortest-mental-model","level":2,"title":"The shortest mental model","text":"<pre><code>flowchart LR\n    A[Python process] --&gt; B[plotsrv]\n    B --&gt; C[Browser UI]</code></pre> <p>Your Python code publishes an object. plotsrv renders it. You view it in the browser.</p>","path":["Get started","What is plotsrv?"],"tags":[]}]}